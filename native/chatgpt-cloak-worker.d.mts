/**
 * Type declarations for chatgpt-cloak-worker.mjs
 * 
 * This module is a CLI worker script that runs as a main process.
 * It doesn't export any functions for external use - it's imported
 * in tests only to verify the module loads without syntax errors.
 */

// Module has no exports - it runs as a main script
export {};
